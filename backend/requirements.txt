# ── Web framework ────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Database ─────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.31.0              # async PostgreSQL driver (prebuilt wheels for Python 3.13)
alembic==1.13.1

# ── Redis ─────────────────────────────────────────────────────
# Use plain redis (no hiredis) to avoid C-extension compilation on Windows.
# Switch to redis[hiredis] in production Linux environments for max throughput.
redis==5.0.4

# ── Authentication ─────────────────────────────────────────────
authlib==1.3.0
python-jose[cryptography]==3.3.0
# passlib[bcrypt] is NOT used in Phase 1 (JWT auth does not require bcrypt).
# Add it back in Phase 7 if password-based login is required.
httpx==0.27.0                # OAuth HTTP client
itsdangerous==2.2.0          # Required by Starlette SessionMiddleware

# ── Simulation ─────────────────────────────────────────────────
# numpy is used by the simulation engine (Phase 3).
# Install via: pip install numpy --only-binary :all:
# (prebuilt wheel required on Windows without MSVC)
networkx==3.3

# ── Validation & config ─────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1

# ── Testing ─────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
pytest-cov==5.0.0
anyio[asyncio]>=4.3.0        # async test runner backend
